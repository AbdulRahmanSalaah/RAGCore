services:
  mongodb:
    image: mongo:7-jammy
    container_name: mini_rag_mongo
    ports:
      - "27007:27017"
    
    volumes:
      - ./mongodb:/data/db
    
    networks:
      - backend
    
    restart: always

networks:
  backend:
    
